# Intent Engine

Cryptographic validity is necessary but not sufficient. The receiver must also decide whether the declared intent is semantically allowed for the authenticated identity, capability and current context.

Intent authorization is pure policy evaluation where possible. Infrastructure side effects occur only after authorization succeeds.
