# Semantic RFC Linter

Validates the generated RFC collection independently from the generator.

Checks include contiguous numbering, missing/orphan files, title and descriptor alignment, semantic-scope presence, duplicate canonical labels, and index integrity.

```bash
python scripts/lint-semantic-rfcs.py
```
