# Intents

An intent declares an outcome desired by a human or external system without prescribing implementation. An intent may cross several entities and bounded contexts.

This example contains only the two business outcomes needed by the PoC: register products that were purchased and complete a sale that was identified by the payment terminal.
