id: Sale
identity: sale_id
properties:
  sale_id: SemanticId
  amount: CurrencyAmount
  products: SaleItemList
  identified_at: DateTime
states: [identified, products-resolved, completed]
invariants:
  - amount >= 0
  - a sale cannot be completed before its products are resolved
