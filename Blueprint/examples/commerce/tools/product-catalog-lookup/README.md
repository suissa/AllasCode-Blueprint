# ProductCatalogLookup

Resolves a product identifier against the local example catalog. The provider may later be replaced by a database, search service, vector resolver, or remote catalog without changing the semantic capability.
