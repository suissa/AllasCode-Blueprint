# IncreaseStock TypeScript Projection

`implementation.ts` applies a registered purchase to inventory exactly once per `purchase_id`. It never defines orchestration; ordering comes from the `.2flow` definition.
