type: object
required: [sale_id, items]
properties:
  sale_id: { semantic_type: SaleId }
  items:
    type: array
    items:
      required: [product_id, quantity]
      properties:
        product_id: { semantic_type: ProductId }
        quantity: { semantic_type: Quantity }
