# ResolveSaleProducts TypeScript Projection

`implementation.ts` represents the executable projection of product resolution after a sale is identified. The demo supplies already-resolved products; a production binding could replace this projection with human or AI-assisted resolution without changing the Action contract.
