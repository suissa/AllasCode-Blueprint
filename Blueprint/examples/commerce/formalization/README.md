# Formalization

This folder declares domain laws that every projection must preserve. These statements are intentionally implementation-neutral and may later be translated into proof systems, property tests, model checking, or runtime invariants.
