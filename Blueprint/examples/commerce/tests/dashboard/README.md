# Test Dashboard

Static dark dashboard built with HTML, CSS, vanilla JavaScript, Tailwind CDN, Animate.css and inline SVG icons. `data.json` is generated by scanning every `tests/**/result.json`. The browser never decides chart semantics: each result declares its own `presentation.components`.
