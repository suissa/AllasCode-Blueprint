# Executable Specifications

These tests are runtime checks for the semantic example. They verify the most important invariants of the commerce lifecycle: a purchase increases inventory and records its financial effect; a sale decreases inventory and records revenue; and inventory cannot become negative.

Run with `npm test` from the commerce example directory.
