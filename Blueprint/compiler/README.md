# Compiler

The compiler also projects the semantic graph into content-addressed evidence requirements and forge-neutral validation plans. See [FACoP Content-Addressed Evidence](../docs/FACOP-CONTENT-ADDRESSED-EVIDENCE.md).

The compiler parses AllasCode, resolves semantic contracts, checks usage and invocation, and projects implementations to supported backends.

## Pipeline additions

- Step 4.4: Resolving Semantic Identity
- Step 4.5: Resolving Contextual Behavioral Identity

Step 4.5 separates semantic recognition from behavioral completion. It derives canonical characteristics, resolves contextual identity couplings, validates required identities for each behavior, emits proof obligations, and projects coupling edges into the runtime topology.

See [Contextual Behavioral Identity](behavioral-identity/README.md).
