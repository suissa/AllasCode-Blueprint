# Compiler

The compiler also projects the semantic graph into content-addressed evidence requirements and forge-neutral validation plans. See [FACoP Content-Addressed Evidence](../docs/FACOP-CONTENT-ADDRESSED-EVIDENCE.md).

The compiler parses AllasCode, resolves semantic contracts, checks usage and invocation, and projects implementations to supported backends.
