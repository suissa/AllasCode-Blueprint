suite:
  canonical_label: CodeHealer.RevertCandidate.contract
  source: ../specifications/contract.yml
cases:
  - name: active-candidate
    scenario: ../specifications/scenarios.yml#active-candidate
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: unknown-base
    scenario: ../specifications/scenarios.yml#unknown-base
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: duplicate
    scenario: ../specifications/scenarios.yml#duplicate
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authority-boundary
    mutate_outside_declared_scope: true
    expected:
      result: Error
      mutation_applied: false
