suite:
  canonical_label: CodeHealer.PromoteVerifiedCodeCandidate.contract
  source: ../specifications/contract.yml
cases:
  - name: approved
    scenario: ../specifications/scenarios.yml#approved
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: unverified
    scenario: ../specifications/scenarios.yml#unverified
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: scope-escape
    scenario: ../specifications/scenarios.yml#scope-escape
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authority-boundary
    mutate_outside_declared_scope: true
    expected:
      result: Error
      mutation_applied: false
