suite:
  canonical_label: CodeRuntime.CaptureRuntimeEvidence.contract
  source: ../specifications/contract.yml
cases:
  - name: complete-streams
    scenario: ../specifications/scenarios.yml#complete-streams
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: missing-source
    scenario: ../specifications/scenarios.yml#missing-source
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: contradiction
    scenario: ../specifications/scenarios.yml#contradiction
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authority-boundary
    mutate_outside_declared_scope: true
    expected:
      result: Error
      mutation_applied: false
