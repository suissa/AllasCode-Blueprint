suite:
  canonical_label: SystemHealer.StageConfigCandidate.contract
  source: ../specifications/contract.yml
cases:
  - name: bounded-timeout
    scenario: ../specifications/scenarios.yml#bounded-timeout
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: immutable-property
    scenario: ../specifications/scenarios.yml#immutable-property
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: mixed-intervention
    scenario: ../specifications/scenarios.yml#mixed-intervention
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authority-boundary
    mutate_outside_declared_scope: true
    expected:
      result: Error
      mutation_applied: false
