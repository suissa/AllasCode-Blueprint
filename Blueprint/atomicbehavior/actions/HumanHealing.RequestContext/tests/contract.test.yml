suite:
  canonical_label: HumanHealing.RequestContext.contract
  source: ../specifications/contract.yml
cases:
  - name: single-evidence-gap
    scenario: ../specifications/scenarios.yml#single-evidence-gap
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authorization-needed
    scenario: ../specifications/scenarios.yml#authorization-needed
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: unreachable-channel
    scenario: ../specifications/scenarios.yml#unreachable-channel
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authority-boundary
    mutate_outside_declared_scope: true
    expected:
      result: Error
      mutation_applied: false
