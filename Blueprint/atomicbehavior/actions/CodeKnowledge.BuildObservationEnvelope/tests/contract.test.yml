suite:
  canonical_label: CodeKnowledge.BuildObservationEnvelope.contract
  source: ../specifications/contract.yml
cases:
  - name: complete-evidence
    scenario: ../specifications/scenarios.yml#complete-evidence
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: missing-trace
    scenario: ../specifications/scenarios.yml#missing-trace
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: unversioned-intent
    scenario: ../specifications/scenarios.yml#unversioned-intent
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authority-boundary
    mutate_outside_declared_scope: true
    expected:
      result: Error
      mutation_applied: false
