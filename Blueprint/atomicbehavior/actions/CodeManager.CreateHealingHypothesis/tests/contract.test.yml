suite:
  canonical_label: CodeManager.CreateHealingHypothesis.contract
  source: ../specifications/contract.yml
cases:
  - name: verified-exact-case
    scenario: ../specifications/scenarios.yml#verified-exact-case
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: novel-supported-case
    scenario: ../specifications/scenarios.yml#novel-supported-case
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: underdetermined
    scenario: ../specifications/scenarios.yml#underdetermined
    assert:
      - fixed_result_event
      - declared_authority_scope
      - preserved_original_intent
  - name: authority-boundary
    mutate_outside_declared_scope: true
    expected:
      result: Error
      mutation_applied: false
